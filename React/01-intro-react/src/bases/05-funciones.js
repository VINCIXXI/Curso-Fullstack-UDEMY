const saludar=function(nombre){
    return `Hola, ${nombre}`
};

//funcion flecha
const saludar2=(nombre)=>{
    return `Hola, ${nombre}`
};

//otra forma
const saludar3=(nombre)=>{`Hola, ${nombre}`};

//otra
const getUSer=()=>({
    uid:"ABD123",
    usrname:"Elpapi"
});